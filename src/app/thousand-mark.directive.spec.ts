import { ThousandMarksDirective } from './thousand-mark.directive';

describe('ThousandMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new ThousandMarkDirective();
    expect(directive).toBeTruthy();
  });
});
